# HomeworkHelperWebCrawler
Web crawler that returns 'answers' for an array of 'questions' which are broken up to make into urls to feed to search engine, and then have the HTML of the google page parsed to.
